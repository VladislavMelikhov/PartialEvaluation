#pragma once
#include "KRecord.h"
#include <vector>

void performTraversal(
	std::vector<KRecord> const& records,
	const int height,
	int* sum);

void performTraversal(
	std::vector<KRecord> const& records,
	const int height);